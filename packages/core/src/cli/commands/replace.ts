export async function replace(
  globs: string[],
  flags: Record<string, string | boolean>,
): Promise<void> {
  console.log("linguo replace — not yet fully wired");
  console.log("Globs:", globs.length > 0 ? globs : "(from config)");
}
